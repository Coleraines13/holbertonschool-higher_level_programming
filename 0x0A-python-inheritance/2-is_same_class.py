#!/usr/bin/python3
"""is_came_class"""

def is_same_class(obj, a_class):
    """Returns true if object os same insatnce"""
    if type(obj) is a_class:
        return True
    else:
        return False
